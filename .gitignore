# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Entornos virtuales
.venv/
venv/
ENV/
env/

# PyInstaller (autogenera *.spec si no se pasa uno custom).
# El repo tiene app.spec custom: NO se ignora.
*.spec.bak
*.manifest

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Base de datos local (se regenera al ejecutar)
database/database.db
database/*.db

# Logs y backups locales
logs/*.log
logs/*.txt
backups/*.db
backups/*.zip

# Configuracion sensible
config.local.json

# config.json se regenera al primer arranque con defaults
# (no commitear para no subir datos del entorno local)
config.json

# Excel temporal del importador
*.xlsx.import.tmp

# Build de PyInstaller
build/
dist/

# Archivos del usuario (no del proyecto)
Cierre_Turno_V2.xlsm
.~lock.*

# Backups de pruebas del smoke test (HTMLs generados automaticamente)
backups/smoke_informe_*.html
backups/Informe_*.html
backups/muestra_informe_*.html

# Log generado por smoke tests
logs/correo.log

# Lock de uv
uv.lock

# Metadata de uv build
sistema_fds.egg-info/